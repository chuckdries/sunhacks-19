{{#if name}}
<h1>Hello, {{ name }}</h1>
{{else}}
<h1>I don't know what your name is!</h1>
<form method="GET" action="greet">
  <input type="text" name="name" placeholder="Your name" />
  <button>Say hello</button>
</form>
{{/if}}